<h1>Categories, yo</h1>

  <%= link_to 'New Category', new_admin_category_path %>
  <br>
  <br>

  <table>
  <tr>
    <th>Category</th>
  </tr>

      <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
      </tr>
      <% end %>
  </table>

